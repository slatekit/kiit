package slatekit.generator


/**
 * Used for Slate Kit related build settings
 */
data class ToolSettings(val version:String)

/**
 * Used for Kotlin related build settings
 */
data class BuildSettings(val version:String)