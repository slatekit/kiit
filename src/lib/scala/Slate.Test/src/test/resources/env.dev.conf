env.name = "dev"
env.mode = "dev"
env.desc = "shared development environment"

#  Example: Showing that there is no setting here to
#  override from the env.conf

test {
  string = "env dev"
  boolean_false = false
  boolean_true = true
  integer = 2
  double = 20.2
}