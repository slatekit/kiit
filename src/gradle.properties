# Tell IntelliJ not to auto-discover nested Gradle builds in subdirectories.
# The only sub-projects in this build are those explicitly included in settings.gradle.kts.
idea.gradle.includeBuildProjects=false
