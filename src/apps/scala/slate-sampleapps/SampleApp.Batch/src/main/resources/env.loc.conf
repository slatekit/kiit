
# environment info
env {
  name : "loc"
  mode : "dev"
  desc : "local development"
}

# log and level
log {
  name     : "@{app.name}-@{env.name}-@{date}.log"
  enabled  : true
  level    : "debug"
}