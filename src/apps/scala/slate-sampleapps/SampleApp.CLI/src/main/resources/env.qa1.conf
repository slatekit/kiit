
# environment info
env {
  name : "qa1"
  mode : "qa"
  desc : "shared quality assurance environment 1"
}

# log and level
log {
  name     : "@{app.name}-@{env.name}-@{date}.log"
  enabled  : true
  level    : "info"
}